@model IEnumerable<WebApplication5.Controllers.Country>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@foreach (var item in Model)
{
    <div>
        @item.CountryCountry
        <hr />
    </div>
}

